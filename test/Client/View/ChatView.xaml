<Window x:Class="Client.View.ChatView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        xmlns:viewModel="clr-namespace:Client.ViewModel"
        mc:Ignorable="d"
        Title="Chat Channel" Height="800" Width="800">
    <Window.DataContext>
        <viewModel:ChatViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="80*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>

        <GridSplitter Grid.Row="1" Grid.Column="3" Height="2" HorizontalAlignment="Stretch" ResizeDirection="Rows" ResizeBehavior="PreviousAndNext"/>

        <GridSplitter Grid.Column="1" Grid.RowSpan="3"  Width="2" VerticalAlignment="Stretch" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext"/>

        <DockPanel Grid.Column="0" Grid.RowSpan="3" LastChildFill="True">
            <DockPanel DockPanel.Dock="Top" LastChildFill="True">
                <Label DockPanel.Dock="Left" Content="User :"/>
                <Button DockPanel.Dock="Right" Content="Connect" Command="{Binding ConnectCommand}" CommandParameter="{Binding ElementName=txtUser, Path=Text}"/>
                <TextBox x:Name="txtUser" Text="{Binding Message, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </DockPanel>
            <ListView/>
        </DockPanel>

        <ListView Grid.Row="0" Grid.Column="2"/>

        <TextBox Grid.Row="2" Grid.Column="2"/>

    </Grid>
</Window>
